<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:Tessera.ViewModels"
             mc:Ignorable="d" d:DesignWidth="280" d:DesignHeight="600"
             x:Class="Tessera.Views.Settings.SettingsSidebarView">

    <Grid RowDefinitions="Auto,*">
        <!-- Settings Header with Close Button -->
        <Border Grid.Row="0" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="0,0,0,1" Padding="16,12">
            <Grid ColumnDefinitions="*,Auto">
                <TextBlock Grid.Column="0" Text="SETTINGS" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Command="{Binding ToggleSettingsCommand}" Content="✕" Padding="6" FontSize="12" Background="Transparent" BorderThickness="0" ToolTip.Tip="Close Settings"/>
            </Grid>
        </Border>
        
        <!-- Settings Categories List -->
        <ListBox Grid.Row="1" ItemsSource="{Binding SettingsViewModel.Categories}" SelectedItem="{Binding SettingsViewModel.SelectedCategory}" Background="Transparent" Margin="8,8" BorderThickness="0">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding}" Padding="12,8" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}"/>
              </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.Styles>
                <Style Selector="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,2"/>
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </ListBox.Styles>
        </ListBox>
    </Grid>
</UserControl>
